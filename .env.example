# Copy to .env and fill in. .env is git-ignored.
# Optional: Hugging Face read token (https://huggingface.co/settings/tokens).
# The S3DIS backup used by preprocess/prepare_s3dis.py is public; a token only raises rate limits.
HF_TOKEN=
